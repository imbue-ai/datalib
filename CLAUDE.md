# CLAUDE.md

The contributor runbook for this repo is [`AGENTS.md`](AGENTS.md), imported
below. It holds the doc map (start there when a task touches an area you
don't know), the repo layout, how to run the tests, and the conventions
that aren't obvious from the code.

This file exists only so that agents which auto-load `CLAUDE.md` get the
runbook too. Keep the content in `AGENTS.md`; don't duplicate it here.

@AGENTS.md
