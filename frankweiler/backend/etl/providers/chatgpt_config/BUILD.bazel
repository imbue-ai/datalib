load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

# Schema-only config crate for chatgpt_api (Program A goal #1). Bazel-only.
rust_library(
    name = "frankweiler_etl_chatgpt_config",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "frankweiler_etl_chatgpt_config",
    edition = "2021",
    visibility = ["//frankweiler:__subpackages__"],
    deps = [
        "//frankweiler/backend/source_common:frankweiler_source_common",
        "@frankweiler_crates//:anyhow",
        "@frankweiler_crates//:serde",
    ],
)

rust_test(
    name = "chatgpt_config_unittests",
    size = "small",
    crate = ":frankweiler_etl_chatgpt_config",
    edition = "2021",
)
