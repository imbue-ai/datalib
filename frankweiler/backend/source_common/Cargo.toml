# Cargo manifest for the bazel-first crate (see BUILD.bazel). Exists so the
# cargo-built tauri shell can link the ingest-config dependency closure.
[package]
name = "frankweiler-source-common"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true }
