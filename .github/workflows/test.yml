# Runs `bazel test //...` inside the published devcontainer image with
# BuildBuddy as the action cache. Gates merges on PRs and warms the
# BuildBuddy cache on main pushes.
#
# Why a separate workflow (and not a step in `release.yml`): an earlier
# `bazel test //...` step lived in `release.yml` and was removed because
# the `bazel-contrib/setup-bazel` GHA disk cache kept tripping "Lost
# inputs no longer available remotely" against rules_rust's
# ExtractCargoTomlEnvVars actions — every release run rebuilt 1500+
# actions and the broken cache never healed. Routing the cache through
# BuildBuddy instead sidesteps the GHA-cache-shape mismatch entirely.
#
# Why the devcontainer image: `bazel test //...` needs the full Bazel
# build toolchain — clang, cmake, llvm, libclang-dev, lld, perl,
# pkg-config, libssl-dev — that the prod image deliberately omits. The
# devcontainer image carries that toolchain on top of the prod image's
# qmd model cache, latchkey, doltlite CLI, etc. Re-installing it via apt
# at the start of each job (the pattern `release.yml`'s build matrix
# uses) would add ~3 min of apt + cold qmd model download to every run;
# pulling the pre-built image is closer to ~30s.
#
# Bootstrap: the devcontainer image must be published to GHCR before
# this workflow can succeed. The `devcontainer-publish` job in
# `release.yml` publishes it on every `v*` tag push. On a fresh repo
# with no devcontainer image yet, push a tag first; the first PR after
# can then run this workflow.
name: test

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: read  # to pull the devcontainer image from GHCR

# Cancel superseded PR runs — pushing a fixup commit shouldn't keep the
# previous run burning a runner. main pushes don't cancel so the
# BuildBuddy cache fill from every merged commit is preserved.
concurrency:
  group: test-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  bazel-test:
    name: bazel test //...
    runs-on: ubuntu-latest

    # GitHub Actions defaults `run:` steps to `sh` (dash) when the job
    # runs inside a container, which rejects `set -o pipefail` and `[[ ]]`.
    # Force bash so the bash-isms in the steps below work.
    defaults:
      run:
        shell: bash

    # Run the job inside the published devcontainer image. We
    # deliberately track `:latest` rather than pinning a version: the
    # image is only republished on `v*` tag pushes (by
    # `release.yml`'s `devcontainer-publish` job), so `:latest` only
    # moves on deliberate release events, not opportunistically. In
    # exchange, contributors don't have to remember to bump a pin in
    # this file after every release — `.devcontainer/Dockerfile`'s
    # `PROD_IMAGE_TAG` is the only remaining manual pin (and it
    # governs reproducible LOCAL devcontainer builds, which is a
    # property worth keeping).
    #
    # Risk: a broken devcontainer image published at `:latest` taints
    # every subsequent CI run until the next release. Acceptable
    # because release tagging is a deliberate human action; bad images
    # surface immediately on the next PR.
    container:
      image: ghcr.io/imbue-ai/datalib_devcontainer:latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v6
        with:
          # `tools/workspace_status.sh` runs `git describe --tags`; the
          # default shallow clone has no tag history, so describe would
          # always fall back to a bare sha and the `--stamp` outputs
          # would churn on every run.
          fetch-depth: 0

      # Drop the BuildBuddy API key into the workspace `.bazelrc.user`
      # (gitignored) — `.bazelrc`'s `try-import` at the bottom picks it
      # up, and `build --config=buildbuddy` activates the remote cache
      # + BES endpoint. `test` inherits from `build`, so a single
      # `build --config=...` line covers both; adding a separate
      # `test --config=...` line on top double-expands the config and
      # trips bazel's "config expanded more than once" warning.
      #
      # If the secret isn't available (PR from a fork; manual local
      # `act` runs without secrets), the step writes nothing and bazel
      # runs without remote caching — actions still execute locally,
      # just without the cross-machine cache hits.
      - name: Configure BuildBuddy remote cache
        env:
          BUILDBUDDY_API_KEY: ${{ secrets.BUILD_BUDDY_API_KEY }}
        run: |
          set -euo pipefail
          if [[ -n "${BUILDBUDDY_API_KEY:-}" ]]; then
              cat > .bazelrc.user <<EOF
          common --remote_header=x-buildbuddy-api-key=${BUILDBUDDY_API_KEY}
          build --config=buildbuddy
          EOF
              echo "BuildBuddy remote cache enabled for this job."
          else
              echo "BUILD_BUDDY_API_KEY not available — running without remote cache."
          fi

      - name: Show toolchain versions
        run: |
          bazel version
          rustc --version || true

      # FIXME(devcontainer-republish): TEMPORARY bridge — DELETE this whole
      # step once a devcontainer image built after the `sqlite3` Dockerfile
      # addition is published on a `v*` tag. It installs the stock
      # `sqlite3` CLI the beeper provider's extract shells out to
      # (`//tests/fixtures:ingested_tng`); the Dockerfile now lists
      # `sqlite3` in its apt set, but `:latest` only moves on republish, so
      # until then it's installed at runtime here. (Harmless if left in —
      # the `command -v` guard makes it a no-op once the image carries it —
      # but it's dead weight worth removing.) apt lists are pruned in the
      # image, hence the `update`. Runs as root in the container; no sudo.
      - name: Install runtime test deps not yet baked into the image
        run: |
          set -euo pipefail
          if ! command -v sqlite3 >/dev/null 2>&1; then
              apt-get update
              apt-get install -y --no-install-recommends sqlite3
          fi
          echo "sqlite3: $(command -v sqlite3) ($(sqlite3 --version))"

      # `-c opt --config=release` mirrors the build mode `release.yml`'s
      # dist build uses, so the BuildBuddy action cache populated here
      # on every PR / main push warms exactly the entries tag-time
      # release builds will hit (instead of fastbuild entries the
      # release path's opt-mode actions can't use). Side benefit:
      # tests run against the same binaries we ship, which catches
      # opt-mode-only behaviors before release.
      #
      # `--build_metadata=ROLE=CI` tags the invocation in the BuildBuddy
      # UI so CI runs are filterable from contributor invocations.
      # Matches the convention used by `release.yml`'s build + test
      # steps.
      #
      # This step is the merge gate: a non-zero `bazel test` exit must
      # fail the job. It previously carried `continue-on-error: true` to
      # hide a red cross while the CI-only failures were triaged — the
      # two known causes (precommit_test shelling out to a host `cargo`
      # the devcontainer doesn't ship, and exec-config bindgen missing
      # `--host_action_env=LIBCLANG_PATH`) are now fixed at the source,
      # so the suppression is removed and real failures gate merges
      # again.
      #
      # FIXME(devcontainer-republish): the `--host_action_env=LIBCLANG_PATH`
      # half below is a TEMPORARY bridge — DROP both `LIBCLANG_PATH` flags
      # (and the `libdir` resolution above) once a devcontainer image built
      # after the `.bazelrc`/Dockerfile `--host_action_env` additions is
      # published on a `v*` tag.
      #
      # `LIBCLANG_PATH` is passed on the command line for BOTH the target
      # and exec configs. The devcontainer bakes `LIBCLANG_PATH` into
      # `/root/.bazelrc`, but only `--host_action_env` makes it reach
      # exec-config (`[for tool]`) bindgen actions — e.g. boring-sys2,
      # pulled in when an exec-config tool needs the HTTP stack — and the
      # current `:latest` image only baked the `--action_env` half.
      # Computing it here from `llvm-config` (the same source the image
      # uses) means CI is correct regardless of which devcontainer image
      # `:latest` currently points at, instead of waiting for the
      # republish. Mirrors `release.yml`'s `bazelargs` step. Once the
      # republished image carries the baked `--host_action_env` line these
      # flags are a redundant no-op (identical value).
      - name: bazel test //...
        run: |
          libdir="$(llvm-config --libdir)"
          if [[ -z "${libdir}" || ! -d "${libdir}" ]]; then
              echo "ERROR: llvm-config --libdir returned '${libdir}' (not a dir)." >&2
              exit 1
          fi
          echo "Resolved LIBCLANG_PATH=${libdir}"
          # FIXME(e2e): `-//frankweiler/ui:e2e_test` excludes the Playwright
          # e2e suite from the CI gate. It needs `rsync` + a Chromium
          # browser cache the *current* `:latest` devcontainer image does
          # not carry; both are now added to `.devcontainer/Dockerfile`, so
          # once a new image is published on the next `v*` tag, DROP this
          # negative pattern to re-enable the suite as a merge gate. (Local
          # `bazel test //...` is unaffected and still runs it.) The `--`
          # keeps the leading `-` from being read as a flag.
          #
          # qmd model cache: the `//tests/fixtures:ingested_tng_qmd` genrule
          # runs qmd, which reads its ~2GB GGUFs from a host dir mounted
          # into the sandbox. The devcontainer bakes them at
          # `/root/.cache/qmd/models` (prod image's `qmd pull`, run as
          # root). But GitHub Actions overrides HOME=/github/home for
          # container steps, so `.bazelrc`'s `$(HOME)/.cache/qmd/models`
          # mount + build_qmd_index.py's `~/.cache/qmd/models` both point
          # at an EMPTY /github/home path — and qmd re-downloads ~2GB on
          # every run (incl. reruns). Point both at the baked /root cache:
          #   * --sandbox_add_mount_pair makes the baked dir visible in the
          #     genrule sandbox (additive to the .bazelrc $(HOME) mount,
          #     which stays correct for local dev).
          #   * CLAUDE_MIRROR_HOST_HOME makes build_qmd_index.py resolve its
          #     models dir to /root/.cache/qmd/models (the script reads it
          #     in place of $HOME).
          # qmd then finds the baked models + their `.etag` sidecars and
          # skips the download (etag matches the unchanged HF release).
          # Standing CI accommodation — not a republish bridge: the image
          # bakes as root and GH always sets HOME=/github/home.
          bazel test -c opt --config=release --build_metadata=ROLE=CI \
            --action_env=LIBCLANG_PATH="${libdir}" \
            --host_action_env=LIBCLANG_PATH="${libdir}" \
            --sandbox_add_mount_pair=/root/.cache/qmd/models \
            --action_env=CLAUDE_MIRROR_HOST_HOME=/root \
            -- //... -//frankweiler/ui:e2e_test

      # PR-time gate for the fully-static musl release leg. The musl
      # dist build only otherwise runs at tag time (release.yml), and
      # this repo has been burned before by release-only build paths
      # rotting between tags (see the incident notes in release.yml).
      # Building `:dist` under `--config=musl-x86_64` here catches
      # musl-only breakage — a new crate that links a system library,
      # a native build script that mishandles the musl toolchain — on
      # every PR instead of at the next release. It also warms
      # BuildBuddy with the exact opt-mode musl actions the tag-time
      # release build will replay. The static-ness assertion mirrors
      # release.yml's: no PT_INTERP, no DT_NEEDED. (This runs on the
      # x86_64 runner only; the aarch64-musl leg differs by
      # arch, not by build logic, and stays tag-time-only.)
      - name: bazel build //frankweiler/backend:dist (musl static)
        run: |
          libdir="$(llvm-config --libdir)"
          bazel build -c opt --config=release --config=musl-x86_64 \
            --build_metadata=ROLE=CI \
            --action_env=LIBCLANG_PATH="${libdir}" \
            --host_action_env=LIBCLANG_PATH="${libdir}" \
            //frankweiler/backend:dist
          for bin in \
              bazel-bin/frankweiler/backend/dag/datalib_dag \
              bazel-bin/frankweiler/backend/datalib_step/datalib_step \
              bazel-bin/frankweiler/backend/http/frankweiler_http_bin \
              bazel-bin/frankweiler/backend/etl/latchkey_curl_shim; do
              echo "--- ${bin}"
              if readelf -l "${bin}" | grep -q 'INTERP'; then
                  echo "ERROR: ${bin} requests a dynamic loader (PT_INTERP) — not fully static." >&2
                  exit 1
              fi
              if readelf -d "${bin}" 2>/dev/null | grep -q 'NEEDED'; then
                  echo "ERROR: ${bin} has DT_NEEDED shared-library deps — not fully static." >&2
                  readelf -d "${bin}" | grep 'NEEDED' >&2
                  exit 1
              fi
              echo "fully static: OK"
          done
