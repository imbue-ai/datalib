# Surface the devcontainer Dockerfile as a data target so consistency
# tests (//tools:version_pins_test, //datalib/backend:version_consistency_test)
# can read it at test time.
exports_files([
    "Dockerfile",
])
