# BUILD file injected by MODULE.bazel into the @doltlite_amalgamation
# external repo. Exposes the two amalgamation files as bazel targets the
# in-tree `//third-party/doltlite:libsqlite3_doltlite` rule consumes.

exports_files(
    [
        "doltlite.c",
        "doltlite.h",
    ],
    visibility = ["//visibility:public"],
)
