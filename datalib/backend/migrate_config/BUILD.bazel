load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_library", "rust_test")

# `datalib-migrate-config` — rewrites a `config.toml` from a shape the
# runner no longer accepts into the one it does.
#
# This package is the one place a retired config shape is understood.
# Keeping it out of `datalib-dag` / `datalib-http` is the point: the
# shipping programs accept exactly one shape, so a shape we stopped
# writing can't widen what a running pipeline will load. It holds one
# rewrite at a time (today: ungrouped steps → `[[groups]]`); the next
# one replaces it.
rust_library(
    name = "datalib_migrate_config",
    srcs = glob(
        ["src/**/*.rs"],
        exclude = ["src/main.rs"],
    ),
    crate_name = "datalib_migrate_config",
    edition = "2021",
    visibility = ["//datalib:__subpackages__"],
    deps = [
        "//datalib/backend/dag:datalib_dag",
        "@datalib_crates//:anyhow",
        "@datalib_crates//:serde",
        "@datalib_crates//:toml",
    ],
)

rust_test(
    name = "migrate_config_unittests",
    size = "small",
    crate = ":datalib_migrate_config",
    edition = "2021",
    deps = [
        "@datalib_crates//:tempfile",
    ],
)

rust_binary(
    name = "datalib_migrate_config_bin",
    srcs = ["src/main.rs"],
    crate_name = "datalib_migrate_config_bin",
    edition = "2021",
    visibility = ["//visibility:public"],
    deps = [
        ":datalib_migrate_config",
        "@datalib_crates//:anyhow",
    ],
)
