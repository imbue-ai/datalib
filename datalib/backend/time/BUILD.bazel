load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

exports_files(["Cargo.toml"])

# Shared timestamp utilities. Every `now()` and every inbound-string
# parse in the workspace funnels through here so the architectural
# rules ("local-tz offset, not UTC", "never fabricate") live in one
# place. See `src/lib.rs` and `docs/dev/archived/data_architecture_plan.md` §P0.5.
rust_library(
    name = "datalib_time",
    srcs = ["src/lib.rs"],
    crate_name = "datalib_time",
    edition = "2021",
    visibility = ["//datalib:__subpackages__"],
    deps = [
        "@datalib_crates//:anyhow",
        "@datalib_crates//:chrono",
        "@datalib_crates//:serde",
        "@datalib_crates//:thiserror",
    ],
)

rust_test(
    name = "time_unittests",
    size = "small",
    crate = ":datalib_time",
    edition = "2021",
    deps = [
        "@datalib_crates//:serde_json",
    ],
)
