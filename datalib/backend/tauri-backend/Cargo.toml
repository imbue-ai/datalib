[package]
name = "datalib-tauri-backend"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
datalib-core = { path = "../core" }
datalib-schema = { path = "../schema" }
