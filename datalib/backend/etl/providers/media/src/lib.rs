//! `media` — scan a directory tree for audio, images, video and
//! playlists, and record what each one is.
//!
//! This commit is the payload hash and the format classification it
//! rests on; the store, the metadata readers and the scan follow.

pub mod download;
