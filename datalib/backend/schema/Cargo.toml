[package]
name = "datalib-schema"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
datalib-etl-macros = { path = "../etl/macros" }
datalib-time = { path = "../time" }
serde = { workspace = true }
serde_json = { workspace = true }
# For the `BulkUpsertable` trait this crate now owns (see src/bulk.rs).
sqlx = { workspace = true }
