[package]
name = "datalib-runtime"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
name = "datalib_runtime"
path = "src/lib.rs"

# No dependencies, deliberately — see src/lib.rs.
[dependencies]

[dev-dependencies]
tempfile = "3"
