// The specs that rewrite the data root's `config.toml`.
export const CONFIG_MUTATING = [
  "config-error",
  "manager2-browse",
  "manager2-sync",
  "manager2-name",
  "manager2-menu",
  "grid-source-id",
  "sources-view",
  "wizard-select",
  "wizard-email",
  "wizard-slack",
] as const;
