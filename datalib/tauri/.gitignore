# `datalib-http`, `datalib-dag`, `datalib-step` and the two latchkey curl
# binaries staged here by tauri.conf.json's beforeBuildCommand (copied from
# Bazel output) so they're bundled under the .app's Resources, plus the
# `latchkey` wrapper from stage-runtime.sh. Build artifacts — not checked in.
/binaries/
# Node runtime + latchkey/qmd package trees staged by stage-runtime.sh
# (bundled under the .app's Resources/runtime). Build artifacts.
/runtime/
/.runtime-cache/
